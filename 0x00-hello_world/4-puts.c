#include <stdio.h>

/**
 * Main willbe the entrie point for 
 * the c program
 * */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";
	
	
	puts(str);
	return (0);
}
